import { RequestPageClient } from '@/src/features/requests/RequestPageClient';

export const metadata = {
  title: '요청 관리 | 링킷',
};

export default function RequestPage() {
  return <RequestPageClient />;
}
